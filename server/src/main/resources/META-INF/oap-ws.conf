{
  services {
    organization {
      service = ws-organization
      protocol = HTTP
      sessionAware = true
    }
    login {
      service = ws-login
      protocol = HTTP
    }
    auth {
      service = ws-auth
      protocol = HTTP
    }
  }
  interceptors = [security-interceptor]
}
